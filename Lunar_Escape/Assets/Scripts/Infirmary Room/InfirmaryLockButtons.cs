using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class InfirmaryLockButtons : MonoBehaviour
{
    // public TextMeshProUGUI keyboardScreenText;
    // private TextMeshProUGUI currentButtonText;

    // Start is called before the first frame update
    void Start()
    {
        // keyboardScreenText = GetComponent<TextMeshProUGUI>();
        // currentButtonText = GetComponent<TextMeshProUGUI>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
